#cloud-config
local-hostname: ${hostname}
instance-id: ${hostname}
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: ${dhcp4}
      addresses:
        - ${ip_address}/${netmask}
      nameservers:
        addresses: ${nameservers}